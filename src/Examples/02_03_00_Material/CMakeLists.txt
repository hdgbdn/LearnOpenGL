get_filename_component(EXAMPLE_NAME ${CMAKE_CURRENT_LIST_DIR} NAME)
FILE(GLOB TARGET_SOURCES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)

list(APPEND TARGET_LIBS
    HDGBDN
    LOPGL
)
list(APPEND INSTALL_DIRS
    "Shaders")

ADD_SUB_EXAMPLE(${EXAMPLE_NAME} "${TARGET_SOURCES}" "${TARGET_LIBS}" "${INSTALL_DIRS}")